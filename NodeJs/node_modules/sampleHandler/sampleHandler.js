const fs = require( 'fs' )
const moment = require( 'moment' )

module.exports = {
	isFileExists: function ( path )
	{
		return fs.existsSync( path ) ? "true" : "false";
	},
	getMsgWithCurrentDateTime: function ( msg )
	{
		return `${ moment().format('MMMM Do YYYY, h:mm:ss a') } : ${ msg }`;
	}
}